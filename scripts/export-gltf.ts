/**
 * Optional: export a registered procedural asset as GLB.
 * Usage: npx tsx scripts/export-gltf.ts <assetName> <out.glb>
 * (Or import assets from the app after materials.build()).
 */
export {};
